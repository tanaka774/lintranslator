LinTranslator — third-party notices
===================================

LinTranslator itself is MIT licensed (see LICENSE). This file covers everything
it uses or downloads, whose terms are not all the same.

The short version: there is no model licence here to read, because the app ships
no model. Every translation backend talks to an endpoint — a hosted API, or a
model server the user runs themselves — and a model you run is your choice under
its own terms. Nothing LinTranslator downloads for itself is non-commercial.


Downloaded at runtime
---------------------

tessdata_fast language data (eng, jpn, kor, chi_sim, chi_tra, rus, deu, fra,
spa, por, ita, pol, tur, vie, tha, ara)
    Licence: Apache-2.0
    https://github.com/tesseract-ocr/tessdata_fast
    Downloaded on first use, pinned to revision
    87416418657359cb625c412a48b6e1d6d41c29bd and verified against a SHA-256
    before it is installed. See lintranslator/ocr.py:TESSDATA_SHA256.

Nothing else. A translation model is downloaded by the user, for the user's own
server, and is not fetched, converted, bundled or redistributed by this app.


Python dependencies (installed from PyPI, not vendored)
-------------------------------------------------------

pillow          MIT-CMU
jeepney         MIT
pytesseract     Apache-2.0
numpy           BSD-3-Clause     (extras: calibrate, test)
python-xlib     LGPL-2.1-or-later (extra: x11)


Linked GUI libraries (from the distribution, not from PyPI)
------------------------------------------------------------

PyGObject       LGPL-2.1-or-later  the `gi` bindings the GUI is written in
GTK             LGPL-2.1-or-later  linked through PyGObject
pycairo         LGPL-2.1-only      (MPL-1.1 alternative) imported by the picker
                                    to draw the selection rectangle

These are imported, not vendored: they come from the distro because they bind
system libraries. LGPL in this position means the app may be MIT, but a
distributor shipping a modified copy of *them* has obligations the app's own
licence does not cover.


External programs
-----------------

tesseract       Apache-2.0       invoked as a subprocess, not linked
